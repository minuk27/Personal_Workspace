server:
  port: 1111

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/minuk27/MSA_Project_Config.git
          default-label: main
           #search-paths: msa-minuk-config-file/**